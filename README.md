# URL Shortener CLI

Консольный сервис сокращения ссылок с поддержкой TTL, лимитов по кликам, автоматической очистки и прав доступа по UUID.

---

## Поддерживаемые команды

- create <url> [maxClicks] [ttlMinutes] — создать короткую ссылку
Пример: create https://example.com 5 60
- list — показать все ваши ссылки
- open <code> — открыть оригинальный URL в браузере (увеличивает счётчик кликов)
- delete <code> — удалить ссылку (только владелец)
- edit <code> <new_maxClicks> <new_ttl_minutes> — изменить лимит/TTL (только владелец)
- help — список команд
- exit — выйти

---

## Архитектура проекта

src
 ├── cli
 │    ├── App.java               // Точка входа, загрузка UUID и запуск CLI
 │    └── CommandProcessor.java  // Парсер и обработчик пользовательских команд
 │
 ├── core
 │    ├── Config.java            // Загрузка конфигурации из config.properties
 │    ├── UrlShortener.java      // Основная бизнес-логика сокращения ссылок
 │    ├── LinkModel.java         // Модель данных для одной короткой ссылки
 │    ├── LinkCleaner.java       // Фоновый поток очистки просроченных/исчерпанных ссылок
 │    └── ValidationException.java // Исключения валидации входных данных
 │
 ├── storage
 │    └── InMemoryStore.java     // Хранилище ссылок (в памяти, с индексом по пользователям)
 │
 ├── util
 │    └── FileUtils.java         // Работа с файловой системой (UUID, конфиги)
 │
 └── resources
      └── config.properties      // Настройки TTL, лимитов, путей, уведомлений

---

## Тестирование

Покрытие: ≥50% кода (15 простых тестов, JUnit 5).
Файлы тестов находятся в src/test/....
